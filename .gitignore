# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# content/posts/ and static/images/ are generated by scripts/sync_vault.py
# but ARE committed — CI builds from them, so it needs no vault access.
# Run scripts/publish.sh to regenerate and push.

.DS_Store
